# Aggiornamento webapp: conteggio, tasto cima e “dal 2021”

Sostituisci almeno il file `index.html` nella root del repository GitHub con quello contenuto in questa cartella.

Poi esegui **Commit changes**. Vercel pubblicherà automaticamente la nuova versione.

Modifiche incluse:
- il riepilogo mostra solo `N ricette trovate`;
- appare un piccolo pulsante in basso a destra dopo aver scrollato la pagina, per tornare in cima;
- il pulsante sparisce di nuovo quando si torna in alto;
- aggiunta discreta della dicitura `dal 2021` nel riquadro del logo, senza alterare logo o layout.
